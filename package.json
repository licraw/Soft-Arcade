{
  "name": "jquery-tile-game",
  "private": true,
  "scripts": {
    "build:pages": "mkdir -p pages-dist && cp game.html pages-dist/index.html && cp game.html game.css game.js game-utils.js game-config.js jquery-3.4.1.js pages-dist/",
    "deploy:pages": "npm run build:pages && wrangler pages deploy pages-dist --project-name=tile-game-arcade",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "db:create": "wrangler d1 create tile-game-scores",
    "db:migrate:local": "wrangler d1 migrations apply tile-game-scores --local",
    "db:migrate:remote": "wrangler d1 migrations apply tile-game-scores"
  },
  "devDependencies": {
    "wrangler": "^4.14.1"
  }
}
